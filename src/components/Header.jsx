import React from "react";
import MenuBookIcon from '@material-ui/icons/MenuBook';

const Header = (props) => {
  return (
    <header>
      <h1><MenuBookIcon /> Su Notes 🦄 </h1>
    </header>
  );
};

export default Header;
